#include <iostream>
using namespace std;
int main() {
    int money, cnt;
	cin >> money >> cnt;
	cout << money / cnt << endl << money % cnt;
    return 0;
}
// ctrl + alt + c -> build
// ctrl + alt + r -> execute