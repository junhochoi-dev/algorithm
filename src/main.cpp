#include <iostream>
using namespace std;
#define endl '\n'
int main() {
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	freopen("input.txt", "r", stdin);

    return 0;
}
// Ctrl + Alt + N with input.txt

// Ctrl + Alt + C -> compile
// Ctrl + Alt + R -> execute